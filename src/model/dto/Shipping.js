export class Shipping {
  /**
   *
   * @param {object} param0
   * @param {number} param0.id
   * @param {string} param0.name
   */
  constructor({ id, name }) {
    this.id = id;
    this.name = name;
  }
}
